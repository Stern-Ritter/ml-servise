# ml-servise

## Тестирование
1. Запустить систему: `docker compose up -d --build`
2. Установить зависимости тестов: `pip install -r tests/requirements-test.txt`
3. Запустить тесты: `pytest tests/ -v`