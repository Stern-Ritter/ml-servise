events {
    worker_connections 100;
}

http {
    include /etc/nginx/mime.types;
    resolver 127.0.0.11 ipv6=off;

    server {
        listen 80;

        root /usr/share/nginx/html;

        # Static assets must exist as files; otherwise return 404 (do not fallback to index.html).
        location ~* \.(?:css|js|mjs|map|png|jpg|jpeg|gif|svg|ico|webp|woff|woff2|ttf)$ {
            try_files $uri =404;
            expires 1h;
            add_header Cache-Control "public";
        }

        location /api/ {
            proxy_pass http://app:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location /docs {
            proxy_pass http://app:8000/docs;
        }

        location /redoc {
            proxy_pass http://app:8000/redoc;
        }

        location /openapi.json {
            proxy_pass http://app:8000/openapi.json;
        }

        location /health {
            proxy_pass http://app:8000/health;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}