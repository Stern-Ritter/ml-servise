/* global window */
// Static config for the UI. You can override API prefix in the Settings page.
window.APP_CONFIG = {
  API_PREFIX: "/api/1.0",
  PREDICT_COST: 100,
  POLL_INTERVAL_MS: 1200,
  POLL_TIMEOUT_MS: 60000,
};

